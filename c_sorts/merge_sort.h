// Header file for our trivial sort functions
// These work on arrays of integers
#include <stdlib.h>

int *merge_sort(int *arr, size_t arr_len);
int *merge(int *arr1, size_t n1, int *arr2, size_t n2);
